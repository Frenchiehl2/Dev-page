import md from './certificate.md';

/*
 * One file served to both languages: certificate names and issuers are proper
 * nouns, so there is nothing to translate.
 */
export default { en: md, de: md };
